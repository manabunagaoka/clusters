// Archived former metrics page. Keeping for reference; route removed by renaming original file.
export default function ArchivedMetrics() {
  return (
    <div style={{padding:24}}>
      <h2 style={{marginTop:0}}>Metrics (Archived)</h2>
      <p style={{fontSize:14, lineHeight:1.5}}>The standalone metrics step has been removed. Clusters now lead directly to Insights. This file is retained only for developer reference and can be deleted once no longer needed.</p>
    </div>
  );
}
